<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Quiz Result</title>
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background-color: #0F1521;
            color: white;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            min-height: 100vh;
            margin: 0;
            padding: 20px;
            text-align: center;
        }
        .result-container {
            background-color: #1E2A3A;
            padding: 30px;
            border-radius: 10px;
            max-width: 600px;
        }
        h1 {
            color: <%= isCorrect ? '#4CAF50' : '#FF6B6B' %>;
            margin-bottom: 20px;
        }
        .result {
            font-size: 24px;
            margin-bottom: 20px;
        }
        .details {
            font-size: 18px;
            margin-bottom: 30px;
            color: #B8C6D9;
        }
        .btn {
            background-color: #748CAB;
            color: white;
            border: none;
            border-radius: 25px;
            padding: 12px 25px;
            font-size: 18px;
            cursor: pointer;
            transition: all 0.2s;
            text-decoration: none;
            margin: 10px;
            display: inline-block;
        }
        .btn:hover {
            opacity: 0.9;
            transform: scale(1.05);
        }
    </style>
</head>
<body>
    <div class="result-container">
        <h1><%= isCorrect ? 'Correct!' : 'Incorrect' %></h1>
        <div class="result">
            Your answer: <%= selected %>
        </div>
        <div class="details">
            <% if (!isCorrect) { %>
                The correct answer was: <%= correct %>
            <% } %>
        </div>
        <div>
            <a href="/quiz" class="btn">Back to Quiz</a>
            <a href="/quiz/test" class="btn">Next Question</a>
        </div>
    </div>
</body>
</html> 